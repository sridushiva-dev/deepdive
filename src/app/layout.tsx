import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Deep Dive — Never stop diving deeper",
  description:
    "Every dive builds your constellation — a living map of how deep you've gone in learning.",
  openGraph: {
    title: "Deep Dive",
    description: "The ocean of learning has no bottom.",
    siteName: "Deep Dive",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
